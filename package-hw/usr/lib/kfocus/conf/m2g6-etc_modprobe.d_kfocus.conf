# THIS IS A MACHINE GENERATED FILE. DO NOT EDIT.
# REMOVE THE OWNING PACKAGE kfocus-hw TO REMOVE THIS FILE.
# KFOCUS_TEST_ID=2231,5416
#
# Sound (m1, m2g1-m2g6)
options snd-hda-intel model=clevo-p950
# options snd-hda-intel model=clevo-p950 probe_mask=1

# Power management
options nvidia          NVreg_DynamicPowerManagement=0x02
options nvidia-drm      modeset=1

# SET 1:
# MUX on Nvidia-only:
# WITHOUT nvidia sleep-resume daemons, will NOT sleep.
#         The preserveVideoMemory stuff requires daemons per syslog.
# WITH    nvidia sleep-resume daemons, will sleep, will NOT resume
#   (Black screen, no cursor; no VTs)
#
# /proc/driver/nvidia/suspend =======================
# https://download.nvidia.com/XFree86/
#   Linux-x86_64/570.86.16/README/powermanagement.html
# This section is specific to the /proc/driver/nvidia/suspend interface.
# This is required if using the NVreg_PreserveVideoMemoryAllocations=1 kernel
# module parameter or advanced CUDA features (such as UVM). The NVIDIA Linux
# kernel driver requires no configuration if the default power management
# mechanism is used.
#
# options nvidia-drm      modeset=1
# options nvidia          NVreg_PreserveVideoMemoryAllocations=1
# options nvidia          NVreg_TemporaryFilePath=/var/tmp
# options nvidia-modeset  vblank_sem_control=0

# SET 2:
# MUX on Nvidia-only:
# WITHOUT nvidia sleep-resume daemons, will sleep, will NOT resume
#   (Black screen, no cursor; no VTs)
# WITH    nvidia sleep-resume daemons, same as above
#
# options nvidia          NVreg_DynamicPowerManagement=0x02
# options nvidia-drm      modeset=0
# options nvidia-modeset  vblank_sem_control=0

# SET 3:
# MUX on Nvidia-only:
# WITHOUT nvidia sleep-resume daemons, ???
#   (Black screen, no cursor; no VTs)
# WITH    nvidia sleep-resume daemons, ???
#
# options nvidia-drm      modeset=1
# options nvidia-modeset  vblank_sem_control=0


#####################################################
# Enable all sleep daemons
# sudo systemctl enable nvidia-hibernate.service nvidia-resume.service nvidia-suspend-then-hibernate.service nvidia-suspend.service
#
# # Disable
# sudo systemctl disable nvidia-hibernate.service nvidia-resume.service nvidia-suspend-then-hibernate.service nvidia-suspend.service
