# See man apt_preferences
#
# THIS IS A MACHINE GENERATED FILE. PLEASE DO NOT EDIT.
#   IF YOU CHANGE THIS FILE, IT MAY BE OVERWRITTEN BY A
#   FUTURE UPDATE OF THE 'kfocus-nvidia' PACKAGE.
#   KFOCUS_TEST_ID=1605,3600

# You may remove the package 'kfocus-nvidia' to remove this file;
#   however, doing so will adjust the mix of nvidia driver libs
#   which may prevent the deep-learning suite ('kfocus-conda'),
#   compute, and other ML tasks from working as expected.

# Pair pinning with a specific kfocus-nvidia package version.
# IMPORTANT: This must be done so that the pinning package is
# installed first, then the supporting drivers are installed on the
# next upgrade.
#
Package: kfocus-nvidia
Pin: release v=24.04, n=noble
Pin: origin "ppa.launchpadcontent.net"
Pin: version 24.04.2~beta4
Pin-Priority: 1001

# Use Nvidia supplied versions of nvidia-settings, nvidia-modrobe, and
#   libnvctrl0 by default (priority 1000). These are frequently OOD in Ubuntu
#   'noble' repos; however, they will be selected from 'noble' IFF they
#   are the correct version (priority 1001). This is set in the final
#   stanza of this file.
# IMPORTANT! THIS MUST COME BEFORE the BLACKLIST match below to work.
# Alternate selector: Pin: release l=NVIDIA CUDA
#
Package: nvidia-settings nvidia-modprobe libxnvctrl0
Pin: origin "developer.download.nvidia.com"
Pin: version 535.*
Pin-Priority: 1000

# BLACKLIST (alway ignore) nvidia-driver packages from the NVIDIA
#   repository (Priority -1) execpt for specific files such as
#   nvidia-modprobe and nvidia-settings are above.
# Alternate selector: Pin: release l=NVIDIA CUDA
#
Package: nvidia-* libnvidia-* xserver-xorg-*
Pin: origin "developer.download.nvidia.com"
Pin-Priority: -1

# Pin cuda-drivers to use version 535.230 because we found sometimes later
#   version to require newer packages not available in ubuntu repo. This was
#   last updated on 2025-05-19, and 535.230 is the latest cuda-drivers version
#   on that date. The 1001 Priority causes a version to be installed even if
#   it does not come from the target release ('noble') and forces a downgrade
#   if required.
#
#   This can be updated occassionally because the metapackage
#   description indicates >= for required package.
#   See apt-cache show nvidia-drivers-535 to see which drivers are required,
#   and apt-cache policy nvidia-driver-535 to see installed ubuntu version.
#
# The 1001 Priority causes a version to be installed even if
#   it does not come from the target release ('noble') and forces a downgrade
#   if required.
#
# IMPORTANT! THIS MUST COME BEFORE the cuda* match below,
#   and PRIORITY MUST BE >= 1001 for this to work.
# Alternate selector: Pin: release l=NVIDIA CUDA
#
Package: cuda-drivers cuda-drivers-535
Pin: origin "developer.download.nvidia.com"
Pin: version 535.230.*
Pin-Priority: 1001

# Prefer all cuda* 535 packages from NVIDIA over any Ubuntu packages of the
#   same name and version from any other respository. The 1000 Priority
#   causes a version to be installed even if does not come from the target
#   release ('noble').
# Alternate selector: Pin: release l=NVIDIA CUDA
#
Package: cuda*
Pin: origin "developer.download.nvidia.com"
Pin: version 535.*
Pin-Priority: 1000

# Favor all nvidia-driver packages from the target release ('noble').
#   The 1000 Priority causes a new version from this origin to be installed
#   unless there is the same version from a target release. Since this IS the
#   target release specification, and the 1000 Priority forces a downgrade
#   and prevents packages from proceeding beyond 535.
#
Package: nvidia-* libnvidia-* xserver-xorg-*
Pin: release l=Ubuntu, v=24.04, n=noble
Pin: version 535.*
Pin-Priority: 1000

# Use Ubuntu supplied versions of nvidia-settings, nvidia-modrobe, and
#   libnvctrl0 if available from the 'noble' repository (priority 1001)
#   instead of Nvidia (1000) IFF they are the correct version.
#   However, these packages are frequently OOD in Ubuntu repos.
#   Use `apt-cache policy nvidia-settings nvidia-modprobe libnvctrl0`
#   to see current Ubuntu versions.
#
Package: nvidia-settings nvidia-modprobe libxnvctrl0
Pin: release l=Ubuntu, v=24.04, n=noble
Pin: version 535.*
Pin-Priority: 1001

