#!/bin/bash
# Copyright 2026 MindShare Inc.
#
# Written for the Kubuntu Focus by M. Mikowski, A. Rainbolt
#
# Name    : kfocus-pl-gamemode
# Purpose : GUI for enabling and disabling kfocus-pl-gamemode.service.
# License : GPL v2
# Run By  : Users
# Spec    : 6789
#
set -u;

## BEGIN _importCommonFn {
# Summary   : _importCommonFn
# Purpose   : Load common routines, prefer relative dir
# Example   : _importCommonFn;
# Arguments : none
# Globals   : Package vars _baseDir _baseName
# Outputs   : none
# Returns   : none
#
_importCommonFn () {
  declare _lib_list _is_loaded _lib_file;
  _lib_list=(
    "${_baseDir}/lib/common.2.source"
    '/usr/lib/kfocus/lib/common.2.source'
  );
  _is_loaded='n';
  for _lib_file in "${_lib_list[@]}"; do
    if [ -r "${_lib_file}" ]; then
      # shellcheck source=/usr/lib/kfocus/lib/common.2.source
      if source "${_lib_file}"; then
        _is_loaded='y';
        break;
      fi
    fi
  done

  if [ "${_is_loaded}" = 'n' ]; then
    echo 1>&2 "${_baseName}: ABORT - Cannot source common lib";
    exit 202;
  fi
}
## . END _importCommonFn }

## BEGIN _assignGlobalUiVarsFn {
_assignGlobalUiVarsFn () {
  declare _bold_str _brow_str _erow_str _header_str _table_str;

  _bold_str='<b style="color:#f7941d">';
  _brow_str='<tr><td style="padding:8px 32px 8px 0">';
  _erow_str='</td></tr>';
  _header_str='<h3 style="color:#f7941d">';
  _table_str='<table style="width:100%;margin-right:32px">';

  _windowTitle='KFocus Panther Lake Game Mode';

  _notSupportedMsg="$(cat <<EOF
${_cm2HtmBeginStr}${_table_str}
${_brow_str}${_header_str}System Not Supported</h3>${_erow_str}

${_brow_str}This tool is only useful on a Kubuntu Focus Ar GEN 1 system. <br>
Please uninstall the kfocus-pl-gamemode package.${_erow_str}</table>
${_cm2HtmEndStr}
EOF
  )";

  _errorMsg="$(cat <<EOF
${_cm2HtmBeginStr}${_table_str}
${_brow_str}${_header_str}Error</h3>${_erow_str}

${_brow_str}An error was encountered:${_erow_str}

${_brow_str}XXX_ERROR_MSG_XXX${_erow_str}</table>
${_cm2HtmEndStr}
EOF
  )";

  _startMsg="$(cat <<EOF
${_cm2HtmBeginStr}${_table_str}
${_brow_str}${_header_str}Enable Game Mode?</h3>${_erow_str}

${_brow_str}Kubuntu Focus provides this tool to raise power limits <br>
for the Panther Lake Xe GPU. In some cases, frame <br>
rates can be increased by 50%, although it may have <br>
the opposite effect on CPU-intensive games.${_erow_str}

${_brow_str}Enable Game Mode now?${_erow_str}</table>
${_cm2HtmEndStr}
EOF
  )";

  _runningMsg="$(cat <<EOF
${_cm2HtmBeginStr}${_table_str}
${_brow_str}${_header_str}Game Mode Running</h3>${_erow_str}

${_brow_str}The system is currently in Game Mode. You may run your <br>
GPU-intensive task now with maximum performance.${_erow_str}

${_brow_str}This tool will only run on AC. Unplugging the system or <br>
using KFocus Power & Fan to change the CPU frequency <br>
profile will end this mode and revert to standard power <br>
management. You may leave Game Mode at any time by <br>
clicking the button below.${_erow_str}</table>
${_cm2HtmEndStr}
EOF
  )";
}
## . END _assignGlobalUiVarsFn }

## BEGIN _mainFn {
_mainFn () {
  declare _should_start_service _monitor_pid;

  if [ "$(_cm2EchoModelStrFn)" != 'arg1' ]; then
    _cm2PromptUserFn "${_notSupportedMsg}" "${_windowTitle}" 's';
    exit 0;
  fi

  if [ "$(systemctl is-active kfocus-pl-gamemode.service)" != 'active' ]; then
    _should_start_service="$(kdialog \
      --title "${_windowTitle}" \
      --yesno "${_startMsg}" \
      --yes-label='Run Now' \
      --no-label='Cancel' && echo 'y' || echo 'n')";
    if [ "${_should_start_service}" = 'n' ]; then
      exit 0;
    fi

    pkexec "${_gamemodeSetExe}" 'on' || {
      _cm2PromptUserFn \
        "${_errorMsg//XXX_ERROR_MSG_XXX/Could not start Game Mode service}" \
        "${_windowTitle}" \
        's';
      exit 1;
    }
  fi

  # Service is active at this point
  _monitor_pid=''
  trap 'kill "${_monitor_pid}"' EXIT INT TERM HUP;
  (
    while true; do
      if [ "$(systemctl is-active kfocus-pl-gamemode.service)" != 'active' ];
        then kill $$;
      fi
      sleep 5;
    done
  ) &
  _monitor_pid="$!";

  kdialog \
    --title "${_windowTitle}" \
    --msgbox "${_runningMsg}" \
    --ok-label='Stop Now';

  kill "${_monitor_pid}";

  pkexec "${_gamemodeSetExe}" 'off' || {
    _cm2PromptUserFn \
      "${_errorMsg//XXX_ERROR_MSG_XXX/Could not stop Game Mode service}" \
      "${_windowTitle}" \
      's';
    exit 1;
  }

  # Only reset power state if the state is still Low; if it isn't, the user
  #   most likely intentionally changed power state, and we should not
  #   override them.
  if [ "$("${_powerSetExe}" -r)" = 'Low' ]; then
    "${_pstateExe}";
  fi

  exit 0;
}
## . END _mainFn }

## BEGIN Declare global variables {
declare _binName _binDir _baseDir _baseName _assignList _gamemodeSetExe \
  _powerSetExe _pstateExe _windowTitle _notSupportedMsg _errorMsg _startMsg \
  _runningMsg;
## . END Declare global variables }

## BEGIN Run main if script is not sourced {
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
  _binName="$(  readlink -f "$0"       )" || exit 101;
  _binDir="$(   dirname  "${_binName}" )" || exit 101;
  _baseDir="$(  dirname  "${_binDir}"  )" || exit 101;
  _baseName="$( basename "${_binName}" )" || exit 101;

  # Import libs and assign more global vars
  _importCommonFn;

  _assignList=(
    '_gamemodeSetExe|/usr/lib/kfocus/bin/kfocus-pl-gamemode-set'
    '_powerSetExe|/usr/lib/kfocus/bin/kfocus-power-set'
    '_pstateExe|/usr/lib/kfocus/bin/kfocus-pstate'
  );

  if ! _cm2AssignExeVarsFn "${_assignList[@]}"; then
    _cm2ErrStrFn 'Could not assign variables';
    exit 1;
  fi

  _assignGlobalUiVarsFn;

  _mainFn "$@";
fi
## . END Run main if script is not sourced }
