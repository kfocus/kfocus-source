#!/bin/bash
#
# Copyright 2020-2025 MindShare Inc.
# Written for Kubuntu Focus by:
# Michael Mikowski, Erich Eickmeyer, Aaron Rainbolt
#
# Name    : kfocus-kb-color-set
# Purpose : Run-as-root utility to set keyboard LEDs
# License : GPL v2
# Run By  : kfocus-kb-color
# Spec    : 615
#
# Note: This code primarily supports RGB keyboard backlights. It also has
# save, restore, and toggle functionality for white keyboard backlights, but
# intentionally lacks features for selecting backlight intensity. KDE provides
# controls for the latter already.
#
set -u;

## == BEGIN private functions ========================================= {
## BEGIN _importCommonFn {
# Summary   : _importCommonFn
# Purpose   : Load common routines, prefer relative dir
# Example   : _importCommonFn;
# Arguments : none
# Globals   : Package vars _baseDir _baseName
# Outputs   : none
# Returns   : none
#
_importCommonFn () {
  declare _lib_list _is_loaded _lib_file;
  _lib_list=(
    "${_baseDir}/lib/common.2.source"
    '/usr/lib/kfocus/lib/common.2.source'
  );
  _is_loaded='n';
  for _lib_file in "${_lib_list[@]}"; do
    if [ -r "${_lib_file}" ]; then
      # shellcheck source=/usr/lib/kfocus/lib/common.2.source
      if source "${_lib_file}"; then
        _is_loaded='y';
        break;
      fi
    fi
  done

  if [ "${_is_loaded}" = 'n' ]; then
    echo 1>&2 "${_baseName}: ABORT - Cannot source common lib";
    exit 202;
  fi
}
## . END _importCommonFn }

## BEGIN Scrubbers for hex_str, bright_int, bright_byte, enable_int
_echoSafeHexStrFn () {
  declare _hex_str;
  _hex_str="${1:-}";
  if [[ ! "${_hex_str}" =~ ^[0-9a-fA-F]{6}$ ]]; then
    _cm2WarnStrFn "Invalid hex_str |${_hex_str}| set to |${_defaultHexStr}|";
    _hex_str="${_defaultHexStr}";
  fi
  echo "${_hex_str}";
}
_echoSafeBrightIntFn () {
  declare _bright_int;
  _bright_int="$(_cm2EchoIntFn "${1:-}")";
  if (( _bright_int < 0 || _bright_int > 10 )); then
    _cm2WarnStrFn "Invalid bright_int |${_bright_int}| set to |${_defaultBrightInt}|";
    _bright_int="${_defaultBrightInt}";
  fi
  echo "${_bright_int}";
}
_echoSafeByteFn () {
  declare _bright_byte;
  _bright_byte="$(_cm2EchoIntFn "${1:-}")";
  if (( _bright_byte < 0 || _bright_byte > 255 )); then
    _bright_byte="${_defaultBrightByte}";
    _cm2WarnStrFn "Invalid bright_byte |${_bright_byte}| set to |${_defaultBrightByte}|";
  fi
  echo "${_bright_byte}";
}
_echoSafeBoolIntFn () {
  declare _enable_int;
  _enable_int="$(_cm2EchoIntFn "${1:-}")";
  if (( _enable_int < 0 || _enable_int > 1 )); then
    _enable_int=1;
    _cm2WarnStrFn "Invalid enable_int |${_enable_int}| set to |1|";
  fi
  echo "${_enable_int}";
}
_slurpFileFn () {
  declare _file;
  _file="${1:-}";
  if [ -z "${_file}" ]; then
    _cm2WarnStrFn "File argument cannot be empty";
    return 1;
  fi

  if [ ! -f "${_file}" ]; then
		_cm2WarnStrFn "Cannot read non-existent file |${_file}|";
    return 1;
	fi

  head -n1 "${_file}";
}
_writeRegisterFn () {
  declare _file _str _do_create;
  _file="${1:-}";
  _str="${2:-}";
  _do_create="${3:-}";
  if [ -z "${_file}" ]; then
    _cm2WarnStrFn "File argument cannot be empty";
    return 1;
  fi

  if [ ! -f "${_file}" ] && [ -z "${_do_create}" ]; then
    _cm2WarnStrFn "Could not find register |${_file}|";
    return 1;
  fi

  if ! echo "${_str}" > "${_file}"; then
    _cm2WarnStrFn "Trouble writing |${_str}| to |${_file}|";
    return 1;
  fi
}
## . END Scrubbers for hex_str, bright_int, bright_byte, enable_int

## BEGIN _convertBrightIntToByteFn {
# Purpose: Convert value from range 0-10 to 0-255.
#
_convertBrightIntToByteFn () {
  declare _bright_byte;
  _bright_byte="$(_echoSafeByteFn "$(( ${1:-0} * 25 ))")";
  if (( _bright_byte > 237 )); then _bright_byte=255; fi
  echo "${_bright_byte}";
}
## . END _convertBrightIntToByteFn }

## BEGIN _convertBrightByteToIntFn {
# Purpose: Convert value from range 0-255 to 0-10.
#
_convertBrightByteToIntFn () {
  _echoSafeBrightIntFn "$(( "${1:-0}" / 25 ))";
}
## . END _convertBrightByteToIntFn }


## BEGIN _rgbCalcKbdStateFn {
# Purpose: Calculate rgb kbd state and save to _configStateList.
#
_rgbCalcKbdStateFn () {
  declare _slurp_str _bright_byte _bright_int _rgb_file _hex_str _enable_int;

  _slurp_str="$(_slurpFileFn "${_rgbBrightFile}")";
  if [[ "${_modelCode}" =~ ^(m2g5p|zrg1|zrg2)$ ]]; then
    _bright_int="$(_echoSafeBrightIntFn "${_slurp_str}")";
  else
    _bright_byte="$(_echoSafeByteFn "${_slurp_str}")";
    _bright_int="$(_convertBrightByteToIntFn "${_bright_byte}")";
  fi

  _rgb_file="${_ledsDir}/rgb:kbd_backlight/multi_intensity";
  _slurp_str="$(_slurpFileFn "${_rgb_file}")";
  _hex_str="$(_convertRgbToHexFn "${_slurp_str}")";

  # If the kbd has brightness, then the user pressed keys.
  #   In this case, change enable_int to 1.
  [ "${_bright_int}" = '0' ] && _enable_int=0 || _enable_int=1;
  _configStateList=( "${_bright_int}" "${_hex_str}" "${_enable_int}" );
}
## . END _rgbCalcKbdStateFn }

## BEGIN _whiteCalcKbdStateFn {
# Purpose: Calculate white kbd state and save to _configStateList.
#
_whiteCalcKbdStateFn () {
  declare _bright_int _enable_int;

  _bright_int="$(_echoSafeBrightIntFn \
    "$(_slurpFileFn "${_whiteBrightFile}")")";

  # If the kbd has brightness, then the user pressed keys.
  #   In this case, change enable_int to 1.
  [ "${_bright_int}" = '0' ] && _enable_int=0 || _enable_int=1;
  _configStateList=( "${_bright_int}" "${_enable_int}" );
}
## . END _whiteCalcKbdStateFn }

## BEGIN _rgbReadStateFileFn {
# Purpose: Read rgb kbd state file into _configStateList.
#
_rgbReadStateFileFn () {
  declare _calc_file _str _val_list _bright_int _hex_str _enable_int;
  [ -z "${1:-}" ] && _calc_file="${_rgbStateFile}" || _calc_file="$1";

  # Create the state file if needed
  if [ ! -f "${_calc_file}" ]; then
    _rgbCalcKbdStateFn;
    _rgbWriteStateFileFn "${_calc_file}";
  fi

  # Read and process state file contents.
  _str="$(_slurpFileFn "${_calc_file}")";
  IFS=';' read -r -a _val_list <<< "${_str}";

  _bright_int="$(_echoSafeBrightIntFn "${_val_list[0]:-}")";
  _hex_str="$(   _echoSafeHexStrFn    "${_val_list[1]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_val_list[2]:-}")";

  _configStateList=( "${_bright_int}" "${_hex_str}" "${_enable_int}" );
}
## . END _readFileListFn }

## BEGIN _whiteReadStateFileFn {
# Purpose: Read white kbd state file into _configStateList.
#
_whiteReadStateFileFn () {
  declare _calc_file _str _val_list _bright_int _enable_int;
  [ -z "${1:-}" ] && _calc_file="${_whiteStateFile}" || _calc_file="$1";

  # Create the state file if needed
  if [ ! -f "${_calc_file}" ]; then
    _whiteCalcKbdStateFn;
    _whiteWriteStateFileFn "${_calc_file}";
  fi

  # Read and process state file contents.
  _str="$(_slurpFileFn "${_calc_file}")";
  IFS=';' read -r -a _val_list <<< "${_str}";

  _bright_int="$(_echoSafeBrightIntFn "${_val_list[0]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_val_list[1]:-}")";

  _configStateList=( "${_bright_int}" "${_enable_int}" );
}
## . END _whiteReadStateFileFn }

## BEGIN _rgbRestoreKbdFn {
# Purpose: Restores color and brightness for rgb kbds from _configStateList.
#
_rgbRestoreKbdFn () {
  declare _bright_int _rgb_str _rgb_file \
    _enable_int _line_dir _calc_bright_int;

  _bright_int="$(_echoSafeBrightIntFn "${_configStateList[0]:-}")";
  _rgb_str="$(   _convertHexToRgbFn   "${_configStateList[1]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_configStateList[2]:-}")";

  if [ -d "${_ledsDir}" ]; then
    while read -r _line_dir; do
      _rgb_file="${_line_dir}/multi_intensity";
      _writeRegisterFn "${_rgb_file}" "${_rgb_str}";
    done < <(find "${_ledsDir}" -maxdepth 1 -mindepth 1 -type l \
      | grep 'rgb:kbd_backlight'
    );
  fi

  if [ -f "${_rgbBrightFile}" ]; then
    if [ "${_enable_int}" = '1' ]; then
      if [[ "${_modelCode}" =~ ^(m2g5p|zrg1|zrg2)$ ]]; then
        _calc_bright_int="${_bright_int}";
      else
        _calc_bright_int="$(_convertBrightIntToByteFn "${_bright_int}")";
      fi
    else
      _calc_bright_int=0;
    fi
    _writeRegisterFn "${_rgbBrightFile}" "${_calc_bright_int}";
  fi
}
## . END _rgbRestoreKbdFn }

## BEGIN _whiteRestoreKbdFn {
# Purpose: Restores brightness for white kbds from _configStateList.
#
_whiteRestoreKbdFn () {
  declare _bright_int _enable_int _calc_bright_int;

  _bright_int="$(_echoSafeBrightIntFn "${_configStateList[0]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_configStateList[1]:-}")";

  if [ -f "${_whiteBrightFile}" ]; then
    if [ "${_enable_int}" = '1' ]; then
      _calc_bright_int="${_bright_int}";
    else
      _calc_bright_int=0;
    fi
    _writeRegisterFn "${_whiteBrightFile}" "${_calc_bright_int}";
  fi
}
## . END _whiteRestoreKbdFn }

## BEGIN _rgbWriteStateFileFn {
# Purpose: Write _configStateList to the rgb state file.
#
_rgbWriteStateFileFn () {
  declare _calc_file _bright_int _hex_str _enable_int _config_str;
  [ -z "${1:-}" ] && _calc_file="${_rgbStateFile}" || _calc_file="$1";

  _bright_int="$(_echoSafeBrightIntFn "${_configStateList[0]:-}")";
  _hex_str="$(   _echoSafeHexStrFn    "${_configStateList[1]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_configStateList[2]:-}")";
  _config_str="${_bright_int};${_hex_str};${_enable_int}";

  _writeRegisterFn "${_calc_file}" "${_config_str}" '_do_create';
}
## . END _rgbWriteStateFileFn }

## BEGIN _whiteWriteStateFileFn {
# Purpose: Write _configStateList to the white state file.
_whiteWriteStateFileFn () {
  declare _calc_file _bright_int _enable_int _config_str;
  [ -z "${1:-}" ] && _calc_file="${_whiteStateFile}" || _calc_file="$1";

  _bright_int="$(_echoSafeBrightIntFn "${_configStateList[0]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_configStateList[1]:-}")";
  _config_str="${_bright_int};${_enable_int}";

  _writeRegisterFn "${_calc_file}" "${_config_str}" '_do_create';
}
## . END _whiteWriteStateFileFn }

## BEGIN _restoreKbdFromConfigFn {
# Purpose: Dispatches to set kbd from _configStateList.
#   This must be called after the config file is read-in.
#   This function is used for both rgb and white kbds.
#
_restoreKbdFromConfigFn () {
  if [ "${#_configStateList[@]}" -lt 2 ]; then
    _cm2WarnStrFn 'No valid config state available';
    return 202;
  fi

  if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
    _rgbRestoreKbdFn;
  else
    _whiteRestoreKbdFn;
  fi
}
## . END _restoreKbdFromConfigFn }
## == . END private functions ========================================= }

## == BEGIN functions run by mainFn =================================== {
## BEGIN _convertHexToRgbFn {
# Purpose: Converts hex value to RGB triple.
#
_convertHexToRgbFn () {
  declare _input_str _hex_list _bit_str _rgb_list;
  _input_str="$(_echoSafeHexStrFn "${1:-}")";

  mapfile -n3 -t _hex_list< <(fold -w 2 <<<"${_input_str}");

  if [ "${#_hex_list[@]}" != 3 ]; then
    _cm2WarnStrFn 'Bad input';
    return 1;
  fi

  _rgb_list=();
  for _bit_str in "${_hex_list[@]}"; do
    _rgb_list+=( $(( "16#${_bit_str}")) );
  done
  echo "${_rgb_list[*]}";
}
## . END _convertHexToRgbFn }

## BEGIN _convertRgbToHexFn {
# Purpose: Convert RGB triple to hex.
#
_convertRgbToHexFn () {
  declare _input_str _rgb_list _bit_str _byte_str _hex_str;
  _input_str="${1:-}";
  IFS=' ' read -r -a _rgb_list <<<"${_input_str}";

  if [ "${#_rgb_list[@]}" != 3 ]; then
    echo "${_defaultHexStr}";
    return 1;
  fi

  _hex_str='';
  for _bit_str in "${_rgb_list[@]}"; do
    _byte_str="$(_echoSafeByteFn "${_bit_str}")";
    _hex_str+="$(printf '%02x' "${_byte_str}")";
  done

  _echoSafeHexStrFn "${_hex_str}";
}
## . END _convertRgbToHexFn }

## BEGIN _rgbEnableKbdFn {
# Purpose: Enable or disable rgb kbd. Do not change color-bright.
#
_rgbEnableKbdFn () {
  declare _set_int _old_list;

  _set_int="$(_echoSafeBoolIntFn "${1:-}")";
  _readInStateFileFn;

  _old_list=( "${_configStateList[@]}" );

  # If the user has made the kbd visible, keep the calculated state;
  #   otherwise, keep the old state.
  _rgbCalcKbdStateFn;
  if (( _configStateList[0] == 0 )); then
    _configStateList=( "${_old_list[@]}" );
  fi

  # Set the enabled flag, update keyboard colors, write config
  _configStateList[2]="${_set_int}";
  _rgbRestoreKbdFn;
  _rgbWriteStateFileFn;
  return;
}
## . END _rgbEnableKbdFn }

## BEGIN _whiteEnableKbdFn {
# Purpose: Enable or disable white kbd. Do not change brightness.
#
_whiteEnableKbdFn () {
  declare _set_int _old_list;

  _set_int="$(_echoSafeBoolIntFn "${1:-}")";
  _readInStateFileFn;

  _old_list=( "${_configStateList[@]}" );

  # If the user has made the kbd visible, keep the calculated state;
  #   otherwise, keep the old state.
  _whiteCalcKbdStateFn;
  if (( _configStateList[0] == 0 )); then
    _configStateList=( "${_old_list[@]}" );
  fi

  # Set the enabled flag, update keyboard colors, write config
  _configStateList[1]="${_set_int}";
  _whiteRestoreKbdFn;
  _whiteWriteStateFileFn;
  return;
}
## . END _whiteEnableKbdFn }

## BEGIN _rgbKeepStateFn {
# Purpose: Calculate rgb keyboard state and store into default files
#
_rgbKeepStateFn () {
  _rgbCalcKbdStateFn;
  _rgbWriteStateFileFn;
}
## .END _rgbKeepStateFn }

## BEGIN _whiteKeepStateFn {
# Purpose: Calculate white keyboard state and store into default files
#
_whiteKeepStateFn () {
  _whiteCalcKbdStateFn;
  _whiteWriteStateFileFn;
}
## .END _whiteKeepStateFn }

## BEGIN _rgbKeepSuspendStateFn {
# Purpose: Calculate rgb keyboard state and store into a suspend file
#
_rgbKeepSuspendStateFn () {
  _rgbCalcKbdStateFn;
  _rgbWriteStateFileFn "${_rgbSuspendFile}";
}
## . END _rgbKeepSuspendStateFn }

## BEGIN _whiteKeepSuspendStateFn {
# Purpose: Calculate white keyboard state and store into a suspend file
#
_whiteKeepSuspendStateFn () {
  _whiteCalcKbdStateFn;
  _whiteWriteStateFileFn "${_whiteSuspendFile}";
}
## . END _whiteKeepSuspendStateFn }

## BEGIN _readInStateFileFn {
# Purpose: Read state from the default file to populate configStateList
#
_readInStateFileFn () {
  if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
    _rgbReadStateFileFn;
  else
    _whiteReadStateFileFn;
  fi
}
## . END _readInStateFileFn }

## BEGIN _rgbRestoreStateFn {
# Purpose: Read rgb state from the default file and restore to the keyboard
#
_rgbRestoreStateFn () {
  _rgbReadStateFileFn;
  _restoreKbdFromConfigFn;
}
## . END _rgbRestoreStateFn }

## BEGIN _rgbRestoreSuspendStateFn {
# Purpose: Read rgb state from a suspend file and restore it to the keyboard
#
_rgbRestoreSuspendStateFn () {
  _rgbReadStateFileFn "${_rgbSuspendFile}";
  _rgbRestoreKbdFn;
}
## . END _rgbRestoreSuspendStateFn }

## BEGIN _whiteRestoreStateFn {
# Purpose: Read white state from the default file and restore to the keyboard
#
_whiteRestoreStateFn () {
  _whiteReadStateFileFn;
  _restoreKbdFromConfigFn;
}
## . END _whiteRestoreStateFn }

## BEGIN _whiteRestoreSuspendStateFn {
# Purpose: Read white state from a suspend file and restore it to the keyboard
#
_whiteRestoreSuspendStateFn () {
  _whiteReadStateFileFn "${_whiteSuspendFile}";
  _whiteRestoreKbdFn;
}
## . END _whiteRestoreSuspendStateFn }

## BEGIN _rgbListActiveStateFn {
# Purpose: Print current rgb kbd state to STDOUT.
#
_rgbListActiveStateFn () {
  _rgbCalcKbdStateFn;
  ( IFS=';'; echo "${_configStateList[*]}"; )
}
## . END _rgbListActiveStateFn }

## BEGIN _whiteListActiveStateFn {
# Purpose: Print current white kbd state to STDOUT.
#
_whiteListActiveStateFn () {
  _whiteCalcKbdStateFn;
  ( IFS=';'; echo "${_configStateList[*]}"; )
}
## . END _whiteListActiveStateFn }

## BEGIN _rgbSetKbdValsFn {
# Purpose: Set kbd brightness and color from the input string.
#   This function intentionally has no white kbd equivalent.
#
_rgbSetKbdValsFn () {
  declare _set_str _bit_list _bright_int _hex_str _enable_int;

  # Parse input
  _set_str="${1:-}";
  IFS=';' read -r -a _bit_list <<< "${_set_str}";

  # Update _configStateList.
  _bright_int="$(_echoSafeBrightIntFn "${_bit_list[0]:-}")";
  _hex_str="$(   _echoSafeHexStrFn    "${_bit_list[1]:-}")";
  _enable_int="$(_echoSafeBoolIntFn   "${_bit_list[2]:-}")";
  _configStateList=( "${_bright_int}" "${_hex_str}" "${_enable_int}" );

  # Restore and write
  if _rgbRestoreKbdFn; then _rgbWriteStateFileFn; fi
}
## . END _rgbSetKbdValsFn }
## == . END functions run by mainFn =================================== }

## BEGIN _mainFn {
# Summary   : _mainFn <options>
# Purpose   : Dispatch per option to set kbd brightness and color.
#   This does nothing if _cm2EchoModelStrFn 'has_kb_led' echos 'n'.
# Examples  : See below.
# Arguments : Some options take values.
#   Hex colors have the form '<rrggbb>'.
#   Rgb colors have the form '<0-255> <0-255> <0-255>'.
# | -c <hex> : Convert hex color to rgb (debug).
# | -C <rgb> : Convert rgb color to hex (debug).
# | -E 0|1   : Enable kbd OFF or ON, use existing color stored.
# | -k       : Calculate keyboard start and write to the suspend file.
# | -K       : Restore keyboard state from the suspend file.
# | -l       : List active state from kbd <int:0-10>;<hex>;<0-1>.
# | -s       : '<0-10>;<hex>' : Set brightness and color; enable is set to 1.
# | -r       : Read and apply state from the default file.
# | -w       : Calculate keyboard state and write to the default file.
# Globals    : OPTIND is reset to local for arg processing.
# Outputs    : Prints diagnostics to stderr.
# Returns    : 0 on success; no-zero on failure.
#
_mainFn () {
  declare _opt_str;

  # Skip any action if the model does not have kbd LEDs
  if [ "$(_cm2EchoModelStrFn 'has_kb_led')" = 'n' ]; then return; fi

  _opt_str="${1:-}"; shift;

  case "${_opt_str}" in
    '-c') _convertHexToRgbFn "$@";;
    '-C') _convertRgbToHexFn "$*";;
    '-E')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbEnableKbdFn "$@";
      else
        _whiteEnableKbdFn "$@";
      fi
      ;;

    '-k')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbKeepSuspendStateFn;
      else
        _whiteKeepSuspendStateFn;
      fi
      ;;
    '-K')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbRestoreSuspendStateFn;
      else
        _whiteRestoreSuspendStateFn;
      fi
      ;;
    '-r')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbRestoreStateFn;
      else
        _whiteRestoreStateFn;
      fi
      ;;
    '-w')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbKeepStateFn;
      else
        _whiteKeepStateFn;
      fi
      ;;

    '-l')
      if [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        _rgbListActiveStateFn;
      else
        _whiteListActiveStateFn;
      fi
      ;;
    '-s')
      if ! [[ "${_modelCode}" =~ ${_rgbModelRegex} ]]; then
        exit 0;
      fi
      _rgbSetKbdValsFn "$@";
      ;;
    *) _cm2EchoFn "Option ${_opt_str} not supported";;
  esac
}
## . END _mainFn }

## BEGIN Declare and assign global vars {
declare _binName _binDir _baseDir _baseName \
  _configStateList _defaultBrightInt _defaultHexStr \
  _rgbBrightFile _whiteBrightFile _rgbStateFile _modelCode \
  _rgbModelRegex _whiteModelRegex _whiteStateFile _rgbSuspendFile \
  _whiteSuspendFile;

_configStateList=();
_defaultBrightByte=175;
_defaultBrightInt=7;
_defaultHexStr='ffff00';
_rgbBrightFile='/sys/class/leds/rgb:kbd_backlight/brightness';
_whiteBrightFile='/sys/class/leds/white:kbd_backlight/brightness';
_ledsDir='/sys/class/leds';
_rgbStateFile='/var/lib/kfocus/kbd_color_state';
_whiteStateFile='/var/lib/kfocus/white_kbd_bright_state';
_rgbSuspendFile='/var/lib/kfocus/rgb_kbd_suspend';
_whiteSuspendFile='/var/lib/kfocus/white_kbd_suspend';
_rgbModelRegex='^(m1|m2|zr)'
_whiteModelRegex='^(14xe|16xe|ar|xe)'
## . END Declare and assign global vars }

## BEGIN Run main if the script is not sourced {
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
  _binName="$(  readlink -f "$0"       )" || exit 101;
  _binDir="$(   dirname  "${_binName}" )" || exit 101;
  _baseDir="$(  dirname  "${_binDir}"  )" || exit 101;
  _baseName="$( basename "${_binName}" )" || exit 101;

  # Import libs and assign more global vars
  _importCommonFn;

  # Get model code, skip unsupported models, set ITE mode if needed
  _modelCode="$(_cm2EchoModelStrFn 'code')";
  if ! [[ "${_modelCode}" =~ ${_rgbModelRegex} ]] \
    && ! [[ "${_modelCode}" =~ ${_whiteModelRegex} ]]; then
    exit 0;
  fi

  ## Debug Code
  # declare _parent_name _debug_file;
  # _parent_name="$(ps -o comm= ${PPID:-})";
  # _debug_file='/tmp/kfocus-kb-set.log';
  # echo "|${_baseName} $*| called by ${_parent_name}" >> "${_debug_file}";
  # pstree -s "${PPID}" >> "${_debug_file}";
  # _mainFn "$@" |& tee -a "${_debug_file}";
  _mainFn "$@";
fi
## . END Run main if the script is not sourced }
