
Package: linux-modules-usbio-generic${variant:suffix}
Architecture: amd64
Section: kernel
Provides: ${dkms:usbio-modules}
Depends:
 ${misc:Depends},
 linux-modules-usbio-${kernel-abi-version}-generic,
Description: Extra drivers for usbio for the generic flavour
 Install extra signed usbio modules compatible with the generic flavour.
