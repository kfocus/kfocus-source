Package: linux-headers-kfocus${variant:suffix}-kfocus
Architecture: amd64
Section: kernel
Conflicts: linux-headers-kfocus${variant:suffix}
Replaces: linux-headers-kfocus${variant:suffix}
Depends:
 ${misc:Depends},
 linux-headers-${kernel-abi-version}-kfocus,
Description: KFocus Linux kernel headers
 This package will always depend on the latest KFocus kernel headers
 available.

Package: linux-image-kfocus${variant:suffix}-kfocus
Architecture: amd64
Section: kernel
Conflicts:
 linux-image-fb-kfocus${variant:suffix}-kfocus,
 linux-image-kfocus${variant:suffix},
Replaces:
 linux-image-kfocus${variant:suffix},
Provides: ${dkms:ivsc-modules} ${dkms:virtualbox-guest-modules} ${dkms:wireguard-linux-compat-modules} ${dkms:ipu6-modules}
Depends: ${misc:Depends},
 linux-image-unsigned-${kernel-abi-version}-kfocus,
 linux-modules-${kernel-abi-version}-kfocus [amd64],
 linux-firmware [amd64],
 intel-microcode [amd64],
 amd64-microcode [amd64]
Recommends: thermald [amd64]
Description: KFocus Linux kernel image
 This package will always depend on the latest KFocus kernel image
 available.

Package: linux-image-uc-kfocus${variant:suffix}-kfocus
Architecture: amd64
Section: kernel
Conflicts:
 linux-image-uc-kfocus${variant:suffix}
Replaces:
 linux-image-uc-kfocus${variant:suffix}
Depends:
 ${misc:Depends},
 linux-image-uc-${kernel-abi-version}-kfocus,
 linux-modules-${kernel-abi-version}-kfocus,
 linux-firmware,
 firmware-sof-signed [amd64],
 wireless-regdb
Description: KFocus Linux kernel image for Ubuntu Core
 This package will always depend on the latest KFocus kernel image
 available.
 .
 This kernel is suitable for staging into a kernel snap, and is not
 suitable to be used directly on classic systems.

Package: linux-tools-kfocus${variant:suffix}-kfocus
Architecture: amd64
Section: kernel
Provides:
 linux-tools
Conflicts: linux-tools-kfocus${variant:suffix}
Replaces: linux-tools-kfocus${variant:suffix}
Depends: ${misc:Depends},
 linux-tools-${kernel-abi-version}-kfocus
Description: KFocus Linux kernel tools
 This package will always depend on the latest KFocus kernel tools
 available.

Package: linux-kfocus${variant:suffix}-kfocus
Architecture: amd64
Section: kernel
Provides:
 ${test:provides-full-kfocus} ${test:provides-full-preferred},
Conflicts: linux-kfocus${variant:suffix}
Replaces: linux-kfocus${variant:suffix}
Depends:
 ${misc:Depends},
 linux-image-kfocus${variant:suffix}-kfocus (= ${binary:Version}),
 linux-headers-kfocus${variant:suffix}-kfocus (= ${binary:Version})
Recommends: linux-tools-${kernel-abi-version}-kfocus
Description: Complete KFocus Linux kernel and headers
 This package will always depend on the latest complete KFocus Linux kernel
 and headers.

